#include <bits/stdc++.h>
using namespace std;
using i64 = long long;

void solve() {
	int n, q;
	cin >> n >> q;

	vector<int> a(n);
	for (int i = 0; i < n; i++) {
		cin >> a[i];
	}
	string ans;

	//关键在于怎么思考...
	// q > 0; n - 
}

int main() {
	int t;
	cin >> t;

	while (t--) {
		solve();
	}

	return 0;
}